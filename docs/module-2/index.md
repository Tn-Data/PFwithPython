# Module 2 — Python Core

<div class="hero" markdown>
<span class="eyebrow">Coming soon</span>
# Conditionals, functions, and collections
<p class="lede">This module will cover <code>if</code> / <code>elif</code> / <code>else</code>, functions, and Python's collection types (lists, tuples, dictionaries, sets) — turning the selection and iteration logic from Module 1 into real, reusable Python.</p>
</div>

!!! life "Adding this module yourself"
    Follow the same pattern as `docs/module-1/`: one Markdown file per lesson, added to the `nav:` list in `mkdocs.yml`. See the repository `README.md` for the full authoring guide (the `runpy` and `quiz` fences, admonition colors, etc.).
