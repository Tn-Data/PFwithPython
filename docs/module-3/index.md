# Module 3 — Data Analysis

<div class="hero" markdown>
<span class="eyebrow">Coming soon</span>
# Pandas: Series, DataFrames, and real data
<p class="lede">This module will point everything from Modules 1 and 2 at real datasets with pandas — reading CSVs, indexing, and filtering.</p>
</div>

!!! trap "Before you build this one"
    Pyodide can load pandas in the browser (`pyodide.loadPackage(["pandas"])`), but it adds real download weight on top of the Python runtime itself. Test it on the connection your students will actually use before committing to fully in-browser pandas exercises — a Colab badge alongside each lesson is the fallback if it's too slow.
